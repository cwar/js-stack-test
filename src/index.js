// @flow
/* eslint-disable no-console */

import Dog from './dog'

const shepard = new Dog('shepard')

console.log(shepard.bark())

// flow-disable-next-line
// something.flow(doesnt.like).for.instance()
